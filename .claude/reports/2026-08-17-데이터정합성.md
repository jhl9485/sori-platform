# SORI 데이터 정합성 검사 보고서

- 검사일: 2026-08-17
- 검사원: data-check (읽기 전용 서브에이전트)
- 발견: 🔴 6건 / 🟡 10건 / 🟢 8건
- 상태: 미조치 (진호님 승인 대기)

---

## 🔴 즉시 수정 권장 (6건)

### 1. 마이페이지 댓글 수가 실제와 최대 20배 차이
- **위치**: `src/app/my/page.tsx:532`, `:646`
- **현재**: `sub={`${p.categoryLabel} · ❤️ ${p.likes} · 💬 ${p.comments}`}`
- **문제**: 여기만 원본 필드 `p.comments`를 그대로 사용. 나머지 전 화면은 `realCommentCount`(예시 댓글 + 내가 쓴 댓글) 사용.
  | 글 id | 마이페이지 표시 | 카드·상세 표시 |
  |---|---|---|
  | 1 | 28 | 4 |
  | 3 | 87 | 4 |
  | 7 | 96 | 3 |
- **근거**: `src/data/communityPosts.ts:397` / `src/lib/comments.ts:39-41` / `CommunityPostCard.tsx:36`
- **수정 방향**: `realCommentCount(p.id, userCommentCounts)`로 통일

### 2. 커뮤니티 상세 한 화면에 조회수 2개, 값이 다름
- **위치**: `src/app/community/[id]/CommunityDetailClient.tsx:116` vs `:169`
- **문제**: 위쪽은 시드 숫자 그대로, 아래 액션바는 시드+1. 한 화면에 "조회 1,234"와 "👁 1.2K(=1,235)"가 동시 표시. 목록 카드는 1,235이므로 위쪽만 틀림.
- **근거**: `src/lib/metrics.ts:41` 설계 규칙 / `MetricRow.tsx:51` / 다른 5개 버티컬엔 중복 표시 없음
- **수정 방향**: `:116`의 조회수 삭제 또는 +1 계산 적용

### 3. 마이페이지 좋아요 탭의 ❤️ 숫자가 항상 1 적음
- **위치**: `src/app/my/page.tsx:646`
- **문제**: 좋아요 탭 = 내가 누른 글이므로 다른 화면은 시드+1. 마이페이지만 시드값. 글 1: 마이페이지 156 vs 카드 157.
- **근거**: `my/page.tsx:115-118` / `MetricRow.tsx:50` / `communityPosts.ts:398`
- **수정 방향**: `MetricRow` 사용 또는 숫자 제거

### 4. 마감된 채용 공고 상세에 "NEW"·"급구" 배지 표시
- **위치**: `src/app/jobs/[id]/page.tsx:64`, `:67`
- **문제**: 같은 페이지 위쪽에 "🔒 마감된 공고예요" 배너가 있는데 아래에 NEW/급구가 같이 표시. 목록은 막았는데 상세만 안 막음.
- **근거**: 올바른 처리 `src/app/jobs/page.tsx:119` / 재현 데이터 `src/data/jobs.ts:47,49` `:87,90` / `src/lib/jobStatus.ts:16-20`
- **수정 방향**: 상세도 `closed`일 때 배지 숨김

### 5. 부동산 "등록 기간" 필터가 카드 날짜와 다른 기준으로 동작
- **위치**: `src/app/realty/page.tsx:23-38` (`withinPeriod`), 사용처 `:85`
- **문제 A**: 필터는 `r.time` 문자열을 지금 시각 기준으로 해석, 카드 날짜는 고정 기준시각(SEED_EPOCH=2026-07-16) 기준. "1주" 필터에 "2026.07.16"(한 달 전) 매물이 남음.
- **문제 B**: 사용자 등록 매물은 7일 후 `"2026. 8. 17."` 형식이 되는데 정규식이 못 읽어 `computedDays=0` → 아무리 오래돼도 항상 필터 통과
- **근거**: `realty/page.tsx:306` / `src/lib/format.ts:56` / `src/lib/userContent.ts:35`
- **수정 방향**: `resolveISO(r.createdAt, r.time)`의 실제 ISO 날짜로 판정

### 6. 커뮤니티 "읽음" 표시가 두 번째 방문에 다시 "안 읽음"으로 복귀
- **위치**: `src/app/community/[id]/CommunityDetailClient.tsx:28`, `:34-37`
- **문제**: `toggle`은 있으면 지우는 함수. 두 번째로 열면 읽음 기록이 삭제됨.
- **근거**: 올바른 처리 `src/lib/metrics.ts:43-50` (`add` 사용) / `src/lib/storage.ts:46-52`
- **덧붙임**: `"sori_read_posts"`는 `metrics.ts` 규칙을 벗어난 하드코딩 키이며 `VIEW_KEY.community`와 개념 중복
- **수정 방향**: `toggle` → `add`, 또는 `VIEW_KEY.community` + `useMarkViewed`로 통일

---

## 🟡 검토 필요 (10건)

### 7. 데스크탑 우측 "인기글" 조회수만 형식·계산이 다름
- `src/components/layout/DesktopRightLists.tsx:50` — 원본 문자열 그대로. 가운데 피드는 `formatCount(시드+1)`. 우측 "3,401" vs 카드 "3.4K" 동시 표시.

### 8. 커뮤니티 목록에 "N개 게시글"이 2개, 서로 다른 수
- `src/app/community/page.tsx:154`(전체 21) vs `:238`(필터 결과). 라벨이 같아 "21개 게시글"과 "4개 게시글"이 동시 표시. 다른 버티컬은 필터 결과 하나만 표시.

### 9. 마이페이지 "작성" 개수에 내가 등록한 업소 누락
- `src/app/my/page.tsx:127` — `useUserBiz()`를 `:66`에서 불러오는데 합계에서 빠짐. 내 글 탭·개요 탭에도 업소 없음.

### 10. 뉴스 "관련 기사" 참조 깨짐 (12개 기사 영향)
- `newsItems.ts` 마지막 `slice(0, 50)`으로 원본 57건 중 7건(id 28,27,21,20,23,22,29) 잘림. 남은 기사의 `relatedIds`가 잘린 id를 가리켜 "관련 기사" 섹션이 조용히 사라짐. 완전 손실 10건, 부분 손실 2건. 추가로 `:1435` `relatedIds: ["26"]`은 원본에 없는 id.

### 11. 뉴스 `time` 형식 6가지 혼재 → "NEW" 배지 사실상 미작동
- `src/app/news/page.tsx:15-21`. `"2026년 7월"`, `"2026년 1분기"`, 서술형 등 파싱 불가 다수. 파싱되는 것도 최신이 07-25라 3일 조건 충족 0건.
- **수정 방향**: `time` 대신 `publishedAt`(전 57건 ISO 정상) 사용

### 12. 시드 채용 공고 3건 모두 마감일이 게시일보다 과거
- `src/data/jobs.ts:46-47`, `:86-87`, `:124-125`. 게시일 환산 2026-07-16, 마감일 06-30/06-10/06-20.

### 13. "댓글 N개"가 답글을 세지 않음
- `src/lib/comments.ts:7-9`. 글 1: 표시 4개 / 실제 말풍선 6개.

### 14. 검색 결과에 "만료 공고 숨김" 규칙 미적용
- `src/app/search/page.tsx:88-91` vs `src/app/jobs/page.tsx:47`. **현재 시드 데이터로는 재현 안 됨** — 규칙 불일치만.

### 15. 부동산 "입주 가능일"이 이미 지난 날짜인데 상태가 "가능"
- `src/data/realtyItems.ts:87,124,235,273`. 전부 과거 날짜. 형식도 ISO/"즉시 입주 가능"/"협의" 혼재.

### 16. "오늘의 인기 글"/"이번 주 인기글"이 실제로는 전체 기간 순위
- `CommunityFeed.tsx:13-18,24` / `DesktopRightLists.tsx:20-25,39`. 날짜 조건 없이 누적 조회수 순 → 두 곳에 같은 글, 두 달 전 글.

---

## 🟢 참고 (8건)

17. 뉴스 id의 날짜와 `publishedAt` 27건 불일치 — 수집일 vs 발행일이면 **의도된 것**. 화면은 전부 `publishedAt` 사용하므로 문제 없음.
18. `isBreaking: true` 13건, 2월 기사 포함 — 반년 전 기사에 "속보" 배지.
19. `metrics.ts` 규칙 밖 하드코딩 키 2개(`sori_read_posts`, `sori_comment_likes`). **나머지 17개 사용처는 전부 규칙 준수 — 이 부분은 잘 지켜짐.**
20. `src/data/notifications.ts`의 `NOTIFICATIONS`는 import되지 않는 죽은 데이터.
21. `Business.isOpen` 필드 정의·데이터만 있고 화면 사용 0곳.
22. 채용만 좋아요 시드 없음 — `Job`에 `likes` 필드 자체가 없음. **목록·상세 일치하므로 버그 아님.**
23. 뉴스만 목록 카드에 지표 없음 — **내부 모순 없음.** 구조 차이만.
24. `format.ts`의 `timeAgo`(:25), `shortPrice`(:116) 사용처 0곳 — 주석에 "향후 활용" 명시.

---

## ⚪ 확인하지 못한 것

1. 뉴스 본문(`fullContent`) 안 숫자·날짜·정책의 **사실 정확성** — 외부 원문 대조 필요. 구조적 필드만 검증.
2. **실제 브라우저 실행 검증** — 읽기 전용 역할이라 정적 분석만. 특히 🔴 5·6번은 클릭 재현 권장.
3. 업소 16곳 전화번호·주소 유효성 — 데이터 주석에 이미 "확인 필요" 명시.
4. 사용자가 실제로 글/업소를 등록한 상태의 동작 — 코드 경로만 확인.
