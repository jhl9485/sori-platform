# SORI 디자인 일관성 점검 보고서

- 점검일: 2026-08-17
- 담당: 디자인팀 (design-audit, 읽기 전용)
- 발견: 🔴 8건 / 🟡 16건 / 🟢 3건 = **27건**
- 상태: 전건 미조치 (진호님 승인 대기)
- 점검 범위: `components/shared/` 16개 전부, 목록 6종, 상세 6종, 작성 5종, 홈 섹션 4종, `error/not-found/loading`

---

## 🔴 눈에 띄게 어긋난 것 (8건)

### 디-1. 벼룩시장 카드에만 하트가 두 개
- **증상**: 물건 카드 하나에 하트 2개. 하나 누르면 나머지도 같이 바뀜.
- **다수 기준**: 6개 중 5개가 카드당 하트 1개 (MetricRow)
- **어긋난 곳**: `app/flea/page.tsx:117` (직접 그린 하트) + `:133` (공용 MetricRow) — 둘 다 `LIKE_KEY.flea` 사용
- **확인**: `/flea` 와 `/realty` 를 두 탭에 놓고 카드 안 하트 개수 세기 → 벼룩에서 사진 위 하트 누르면 아래 하트도 빨개짐
- **수정 방향**: 117행 하트 제거, MetricRow만 남기기

### 디-2. 커뮤니티 글쓰기만 폭이 좁다 (390px vs 480px)
- **다수 기준**: 작성 5개 중 4개가 `max-w-[480px]`
- **어긋난 곳**: `app/write/page.tsx:241` — 나머지 클래스는 글자 하나까지 동일, 폭 숫자만 다름
- **확인**: 창을 넓히고 `/write` 와 `/flea/write` 비교 → 약 90px 차이
- **주의**: 하단 고정 옵션 바(`write/page.tsx:406`)도 같은 390px라 함께 바꿔야 함

### 디-3. 커뮤니티 글쓰기만 공용 ImageUploader 미사용
- **다수 기준**: 사진 받는 작성 4개 중 3개가 공용 사용 (5장, "대표" 배지, 격자 미리보기)
- **어긋난 곳**: `app/write/page.tsx:49·165·375·408` — 직접 구현, 4장 제한, 대표 배지 없음
- **압축 설정도 다름**: 커뮤니티 1000px/0.7 vs 공용 1200px/0.78 (`ImageUploader.tsx:13-14`)
- **주의**: 커뮤니티는 하단 옵션 바 UI가 전제라 배치 변경이 따라옴 — 범위 먼저 정할 것

### 디-4. 홈 데스크탑에서 섹션 제목 줄이 어긋남
- **다수 기준**: 홈 섹션 4개가 2:2로 갈림 → **기준 없음**
- **가장 나쁜 곳**: `components/home/CommunityFeed.tsx:23`(제목 `px-4`) vs `:31`(카드 `px-4 md:px-6`) — **자기 카드와도 8px 어긋남**
- **확인**: 창 최대로 넓히고 홈에서 "Daily SG 뉴스" 제목 왼쪽 끝에 세로선을 긋고 아래로 내려가며 비교

### 디-5. 상세 제목 크기 6개 모두 제각각 — 기준 없음
| 버티컬 | 크기 | 위치 |
|---|---|---|
| 업소 | 1.2rem | `business/[id]:77` |
| 부동산 | 1.15rem | `realty/[id]:124` |
| 커뮤니티 | 1.1rem | `CommunityDetailClient:89` |
| 뉴스 | 1.1rem | `NewsDetailClient:42` |
| 채용 | 1rem | `jobs/[id]:72` |
| 벼룩 | **0.95rem** | `flea/[id]:142` |
- **벼룩은 제목(0.95rem)이 가격(1.3rem)보다 작아 부제처럼 보임**
- **수정 방향**: 먼저 한 값을 정해야 함 (중앙값 1.1rem, 이미 2곳 사용 중)

### 디-6. "내 것" 초록 배지가 부동산만 알약 모양
- **다수 기준**: 4개 중 3개가 `rounded` + `0.6rem` + `px-[5px] py-[1px]`
- **어긋난 곳**: `app/realty/page.tsx:272` — `rounded-full` + `0.62rem` + `px-2 py-[2px]`
- 색(`#2B7A50`)·위치(`top-2 left-2`)는 4개 동일

### 디-7. NEW 배지가 채용만 다른 색·글꼴
- **다수 기준**: 3곳 중 2곳이 초록(`#2B7A50`) + 기본 글꼴
- **어긋난 곳**: `jobs/page.tsx:119`, `jobs/[id]:66` — `#D04020` + IBM Plex Mono
- 여백·모서리·굵기는 동일. **색과 글꼴만** 다름
- 참고: 채용 카드에서 NEW(#D04020)와 급구(#B07010)가 비슷한 난색으로 붙어 나옴

### 디-25. 🔴 작성 화면에서 키보드 포커스가 전혀 안 보임 (접근성)
- Tab으로 이동해도 **지금 어느 칸인지 화면에 표시 없음**
- 5개 작성 화면의 모든 input/textarea가 `outline-none`인데 대체 표시 없음
- **같은 프로젝트에 올바른 예가 이미 있음**: `app/login/page.tsx:9` — `focus:ring-2 focus:ring-[#D04020]/25`
- **해당 파일**: `write:335·347·358`, `business/write:350·375·387·394·402·458·477·490`, `flea/write:266·310·320·419`, `jobs/write:262·269·347·359·385·408·434·449·464·476·488·500`, `realty/write:318·401·408·415·442·452·488·504·563`, `CommentSection:194·266·461`, `BizReviewSection:97·186`, `JobQuestions:84·162`, `SearchField:28`, `my/page:1045·1073`
- **확인**: `/login` 에서 Tab만 눌러보고(주황 테두리 보임), `/flea/write` 에서 Tab만 눌러보기(아무 표시 없음)

---

## 🟡 검토 필요 (16건)

### 디-8. 필터 칩 생김새가 두 갈래 (테두리 4 / 없음 2)
- 업소·벼룩·채용·부동산 = 흰 알약 + 테두리 / 뉴스·커뮤니티 = 맨 글자
- **선택된 칩은 6개 모두 `#181614`로 동일** — 선택 안 된 상태만 갈림
- **의도 가능성**: 뉴스·커뮤니티는 "게시판 탭", 나머지는 "필터 칩"일 수 있음. 두 화면이 일관돼 실수로 보이지 않음

### 디-9. 채용만 초록 계열 CTA
- 5개 중 4개가 `#D04020`, 채용만 `#2B7A50` — 목록·플로팅·작성완료 **세 곳 모두** 초록
- **의도 가능성 높음** (실수로 한 곳만 바뀐 게 아님). "버티컬별 대표색"인지 "앱 전체 한 색"인지 정할 필요

### 디-10. 커뮤니티 목록 '글쓰기' 버튼만 살짝 큼
- 5개 중 4개가 `0.75rem px-3 py-[6px]`, 커뮤니티만 `0.78rem py-[7px]` (`community/page.tsx:159`)
- 위치도 다름 — 다른 4개는 "결과 개수" 줄, 커뮤니티는 페이지 제목 줄

### 디-11. 등록 완료 말투가 커뮤니티만 다름
- 4개 "~되었습니다!" vs 커뮤니티 "~되었어요!" (`write/page.tsx:225·197`)
- **단, 앱 전반은 "~해요"체** (`OwnerActions:29`, `DetailActions:52`)
- **개수 다수결(4:1)과 앱 전체 톤이 반대 방향** → 진호님이 정할 문제

### 디-12. 벼룩시장에서 같은 대상을 '물건/매물/글' 세 이름으로 부름
- 검색창 "물건" / 개수 "매물" / 버튼 "물건" / 배지 "내 글" / 상세 "매물"·"물건"
- **"매물"은 부동산 전용어라 혼동**. 다른 3개 버티컬은 각자 한 단어로 일관
- **수정 방향**: "물건"으로 통일

### 디-13. 글자형 카드 안쪽 여백 — 커뮤니티만 14px
- 채용·뉴스 `p-4`(16px) vs 커뮤니티 `p-[14px]` — 육안 구분 거의 불가

### 디-14. 상세 소제목 크기·태그 제각각 — 기준 없음
- 0.9rem(채용·부동산) / 0.88rem(업소) / 0.85rem(벼룩·뉴스)
- **태그도 섞임**: `<h1>` 다음 `<h2>` 건너뛰고 `<h3>` 쓰는 곳(업소·부동산·뉴스) → 화면 낭독기 단계 어긋남

### 디-15. 빈 화면 아이콘 규칙 없음 (돋보기 3 / 자기 아이콘 3)
- 채용 🔍 / 뉴스 🔍 / 커뮤니티 🔎(다른 돋보기) / 업소 🏪 / 벼룩 🛍️ / 부동산 🏘️
- 검색어 표시 여부도 갈림 (3곳만 검색어를 보여줌)
- **뼈대는 6개 완전 동일** — 아이콘·문구만 다름

### 디-16. 뉴스·부동산 필터 줄만 공용 ScrollRow 미사용
- 3곳은 오른쪽 끝 페이드로 "더 있음"을 알리는데 뉴스(`news:71`)·부동산(`realty:118·135`)은 없어 잘린 것처럼 보임

### 디-17. 커뮤니티 카드만 마우스 올려도 안 떠오름
- 5개는 `hover:-translate-y-[1px]`, 커뮤니티만 그림자만 (`CommunityPostCard:39`)
- **의도 가능성**: `memo`로 감싸져 있어 성능 목적일 수 있음

### 디-18. 홈 채용 카드 비자 칩 모양이 채용 목록과 다름
- 홈 `rounded-[6px]` + 테두리 vs 채용 2곳 `rounded-full` + 테두리 없음
- 색은 동일. 모양·테두리·크기만 다름

### 디-19. 커뮤니티 목록만 뒤로가기 복원을 직접 구현 (약 50줄)
- 5개는 공용 `useListRestore`, 커뮤니티만 `community/page.tsx:54-105`
- **실제 차이**: 커뮤니티는 검색 중이었으면 스크롤 복원 안 함 → 맨 위로 올라감
- **범위 주의**: 디자인보다 동작에 가까움. 커뮤니티 규칙이 더 정교할 수 있어 의도 확인 필요

### 디-20. 주요 버튼 터치 영역이 권장(44px)보다 작음 — 전 화면 공통
- 작성 "등록" 약 28px / 목록 "등록" 약 30px / 사진 삭제 ✕ 20px
- **플로팅 버튼만 48px로 충족**
- 불일치가 아니라 **공통 기준 문제** → "고칠 것"이 아니라 "기준을 정할 것"

### 디-21. 뉴스만 '더 보기' 없고 부제 글꼴도 다름
- 5개는 12개씩(커뮤니티 10개) 끊어 보여주는데 뉴스는 전부 표시
- 뉴스 부제만 `0.72rem` + IBM Plex Mono
- **의도 가능성**: 항목 수가 적어서일 수 있음. 기사 30건 넘으면 필요

### 디-26. 🟡 `#C0BBB0` 글자는 배경과 거의 구분 안 됨 (접근성)
| 조합 | 명암비 | AA(4.5:1) |
|---|---|---|
| `#C0BBB0` / 흰색 | **1.91:1** | ❌ 기준의 42% |
| `#C0BBB0` / `#F5F3EE` | **1.73:1** | ❌ 기준의 38% |
| `#888070` / 흰색 | 3.91:1 | ❌ 작은 글씨 미달 |
| `#181614` / 흰색 | 18.05:1 | ✅ |
| `#D04020` / 흰색 | 4.73:1 | ✅ |
- **실제 글자로 쓰인 곳**: 글자수 "0/50", "💾 자동 저장 중", 커뮤니티 빈 화면 안내, 홈 뉴스 화살표
- **수정 방향**: `#C0BBB0` → `#888070`만 해도 두 배 개선. 토큰 자체 변경은 영향 넓어 별도 작업

### 디-27. 🟡 확인창·신고창은 Esc로 안 닫힘 (사진 확대창은 됨)
- 팝업 6개 중 Esc 되는 건 `Lightbox` 하나뿐
- `Feedback.tsx:87·146`, `my/page.tsx:886·938·1021` — Esc 처리·`role="dialog"`·`aria-modal` 없음
- **수정 방향**: Lightbox의 처리를 그대로 옮기기 (접근성은 객관 기준이라 Lightbox가 정답)

---

## 🟢 참고 (3건)

### 디-22. 카드 hover 그림자가 벼룩만 미세하게 다름
`0.07` 3곳 vs 벼룩 `0.08`. 육안 구분 거의 불가.

### 디-23. 홈 섹션 제목 크기 표기 두 가지지만 화면은 동일
`text-[1rem]`과 `text-base`는 **둘 다 16px**. 코드 표기만 갈림. (디-4와 헷갈리지 말 것)

### 디-24. 빈 화면 높이가 마이페이지만 짧음
목록 8곳 `py-16`, 마이페이지만 `py-12`. 탭 안 좁은 영역이라 의도로 보임.

---

## ✅ 잘 지켜지고 있는 것 (유지해야 할 기준)

1. **카드 모양** — 6개 전부 `rounded-[14px]` + `border-black/[0.08]`. 한 곳도 안 어긋남
2. **상세 폭** — 6개 전부 `max-w-[680px] mx-auto`
3. **목록 대제목** — 6개 전부 `text-[1.2rem] md:text-[1.4rem] font-bold tracking-tight` (글자 하나까지 동일)
4. **공용 컴포넌트 적용률** — 상세 6개가 PageHeader·DetailActions 100%. 사용자 콘텐츠 상세 5개는 DetailSkeleton·OwnerActions 100%. 사진 상세 3개는 PhotoCarousel 100%
5. **플로팅 버튼** — 5개 전부 위치·크기·aria-label 동일 (한 픽셀도 안 다름)
6. **빈 화면 뼈대** — 8곳 전부 동일 클래스
7. **작성 화면 상단바** — 5개 전부 동일
8. **비활성 버튼** — 5개 전부 `bg-[#F0EDE8] text-[#888070]`
9. **임시저장 복원 배너** — 5개 전부 동일
10. **주요 필터 활성색** — 6개 전부 `#181614`
11. **모든 `<img>`에 alt** — 13곳 전부, 누락 0건
12. **상태색 3종 규칙** — 벼룩·부동산이 완전히 동일한 조합
13. **에러·404 화면** — 같은 틀로 통일
14. **Lightbox는 접근성 모범** — Esc·화살표키·role·aria-modal·44px 터치. 다른 팝업의 기준으로 삼을 만함
15. **MetricRow 정렬 처리** — em 단위 + leading-none + 이모지 0.5px 보정으로 카드·상세 중심이 정확히 겹침

---

## ⚪ 확인하지 못한 것

1. **실제 브라우저 렌더링** — 정적 코드 대조만. 눈으로 봤을 때 설명과 다르면 지적이 틀린 것
2. **숫자·데이터 정합성** — 데이터팀 담당이라 미확인
3. **동작·성능** — 범위 밖 (디-19만 경계에 있어 "의도 확인 필요"로 표기)
4. **버티컬 밖 화면 정밀 점검** — `my`(1118줄), `search`(365줄), `notifications`, `login`, `signup`, `terms`, `privacy`는 참고용으로만 훑음. 필요하면 별도 요청
5. **지시서 오류 발견** — 지시서에 `EmptyState`가 공용 컴포넌트로 적혀 있었으나 `shared/`에 없음. `app/my/page.tsx:289`에 그 파일 전용 함수로만 존재. → **지시서 수정 완료**
