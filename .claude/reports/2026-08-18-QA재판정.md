# SORI QA 재판정 보고서

- 점검일: 2026-08-18
- 담당: QA팀 (qa-check, 읽기 + Bash 읽기전용)
- **기준 커밋**: `02995a5`
- 판정 대상: 데이터팀 24 / 디자인팀 27 / 기획팀 33 = **총 84건**
- 결과: ✅ 확인 **81** / ❌ 틀림 **2** / ⚠️ 낡음 **1** / ⚪ 판정불가 **0**
  - 데이터팀 24 = ✅23 + ❌1
  - 디자인팀 27 = ✅25 + ❌1 + ⚠️1
  - 기획팀 33 = ✅33
- 타입·lint: 통과 (`npx tsc --noEmit`, `npx next lint`)

> ⚠️ **QA팀 사고 보고**: 정정 지시 수령 전에 지시서대로 `npm run verify`를 1회 실행했고,
> `verify.cjs:22`의 `kill-port 3000`이 진호님 dev 서버를 종료시켰다. 결과 자체는 통과(exit 0, 24개 라우트).
> 정정 후 재실행하지 않았고 코드는 한 줄도 수정하지 않았다. → **팀장 원인. 지시서 및 CLAUDE.md 정정 완료.**

---

## ❌ 틀린 지적 (2건)

### 디-22 — 그림자 "0.07 3곳 vs 벼룩만 0.08"
**실제는 세 갈래.** 벼룩은 유일한 예외가 아니다.

| 버티컬 | 그림자 | 위치 |
|---|---|---|
| 업소·채용·부동산 | `0_4px_16px` **0.07** | business:111 / jobs:110 / realty:262 |
| 벼룩 | `0_4px_16px` **0.08** | flea/page.tsx:91 |
| 뉴스·커뮤니티 | `0_4px_12px` **0.06** | news:90 / CommunityPostCard:39 |

0.06이 2곳 더 있고 **흐림 반경도 16px/12px로 갈린다.** 지적문은 0.06을 언급하지 않아
"벼룩만 고치면 통일된다"는 잘못된 결론으로 이어진다.

**"낡음"이 아니라 "틀림"인 근거**: 보고 당시 커밋 `1abd072`에서 `git show`로 확인 —
0.06도 0.08도 그때 이미 존재했다. 이후 수정으로 어긋난 게 아니라 처음부터 빠뜨린 것.

### 데-18 — "isBreaking 13건, **2월 기사 포함**, 반년 전"
**개수 13건은 맞으나 2월 기사는 단 한 건도 속보가 아니다.**

13건의 `publishedAt` 전수: `06-04, 06-09, 06-10, 06-10, 06-15, 06-17, 06-17, 06-18, 06-19, 06-22, 06-24, 06-24, 07-05`
→ 가장 오래된 속보가 **2026-06-04**(약 2.5개월 전). "반년 전"이 아니다.
2월 기사는 존재하지만 **전부 `isBreaking: false`** 이고, `slice(0,50)`으로 잘려 화면에 나오지도 않는다.

문제의식 자체는 유효하나 **근거가 사실이 아니라 심각도를 과대평가하게 만든다.**

---

## ⚠️ 낡은 지적 (1건)

### 디-25 — **절반만 수정됨**
커밋 `0d8d600`이 작성 5개 화면만 고쳤다. 원 지적에 함께 적힌 **공용 컴포넌트 10곳은 그대로**다.

**수정 완료 (5곳)** — `outline-none` 개수 = `focus:ring` 개수 일치 확인
write 3 / business-write 8 / flea-write 4 / jobs-write 12 / realty-write 9

**미조치 (10곳)** — 줄번호도 안 밀림
| 파일 | 줄 | focus:ring |
|---|---|---|
| `CommentSection.tsx` | 194, 266, 461 | 0 ❌ |
| `BizReviewSection.tsx` | 97, 186 | 0 ❌ |
| `JobQuestions.tsx` | 84, 162 | 0 ❌ |
| `SearchField.tsx` | 28 | 0 ❌ |
| `my/page.tsx` | 1045, 1073 | 0 ❌ |

→ **댓글·리뷰·질문·검색창·프로필 편집은 여전히 Tab을 눌러도 어디인지 안 보인다.**
커밋 메시지가 범위를 정직하게 밝히고 있어 의도적 부분 수정이나, 항목은 아직 닫히지 않았다.

---

## ✅ 수정 검증 — 14건 전부 통과

### 데이터팀 6건
| 번호 | 검증 근거 |
|---|---|
| 데-1 | `my/page.tsx:512` → `realCommentCount(...)`. 카드·상세와 같은 식 |
| 데-2 | 수정 전(`f2db390`)엔 `· 조회 {formatCount(post.views)} ·` 존재. 현재 제거, 조회수는 `DetailActions` 한 곳뿐 |
| 데-3 | `my/page.tsx:511` → `toNum(post.likes) + (isLiked(post.id) ? 1 : 0)` |
| 데-4 | `jobs/[id]:67·70` 둘 다 `!closed &&` 추가됨 |
| 데-5 | `realty/page.tsx:31-41`가 `resolveISO(createdAt, time)` 사용. 문제 A·B 모두 해소 |
| 데-6 | `toggle` → `add`(멱등) 변경 확인 |

> **데-6 잔여**: `"sori_read_posts"`는 여전히 하드코딩 키 (데-19와 동일 내용)

### 디자인팀 7건 (디-25 제외)
| 번호 | 검증 근거 |
|---|---|
| 디-1 | 사진 위 하트 제거, `MetricRow` 하나만 남음 |
| 디-2 | 본문·하단바 둘 다 `max-w-[480px]`. 작성 5개 전부 동일 |
| 디-3 | 공용 `ImageUploader` 사용. 압축 설정도 자동 통일(1200px/0.78) |
| 디-4 | 홈 4개 섹션 전부 `px-4 md:px-6` |
| 디-5 | 6개 `<h1>` 전부 `text-[1.1rem]` |
| 디-6 | `rounded` + `0.6rem` + `px-[5px] py-[1px]` — 나머지 3곳과 완전 동일 |
| 디-7 | 목록·상세 둘 다 `#2B7A50` + 기본 글꼴 |

> **디-7 잔여**: "급구" 배지는 여전히 IBM Plex Mono. 원 지적은 NEW 한정이라 항목은 닫아도 됨

---

## 📌 세부 정정 (지적은 맞으나 수치·범위가 어긋난 것)

- **데-15** 4개 줄 전부 과거 날짜는 맞으나 **`status: "가능"`은 4건 중 2건**(id 1, 6). id 2는 "예약중", id 5는 "완료".
  또 `"협의"`는 `realtyItems.ts`에 없고 사용자 등록 폴백값(`userContent.ts:254`)이다.
- **데-19** 하드코딩 키 2종은 맞으나 "나머지 17개"는 실제 **21개**
- **디-8** "선택 칩 6개 모두 `#181614`"는 **주 필터에 한함** — `jobs:78`(고용형태) `#2B7A50`, `business:79·84`(음식종류) `#D04020`
- **디-15** 커뮤니티는 **검색 중일 때만 🔎, 평소엔 ✏️** (조건부)
- **디-24** "목록 8곳"은 실제 **10곳**
- **디-27** "Esc 되는 건 Lightbox 하나뿐"은 부정확 — `MobileDrawer.tsx:47·68·69`도 Esc + role + aria-modal 갖춤
- **기-13** "나머지 4곳"은 실제 **3곳** (jobs/write엔 사진 기능 자체가 없음)
- **기-24** 파일 길이 84줄 → 실제 **83줄**

## 📌 줄번호 밀림 (내용은 정확)
데-9 `127→129` · 디-11 `225·197→186·158` · 디-27 `886·938·1021→887·939·1022` · 기-16 `47-53→44-52`

---

## 🔧 정적 건강도

- **깨진 내부 링크: 0건** — 정적 18종 + 동적 17종을 `src/app/` 구조와 전수 대조
- **깨진 참조: 1건** — `newsItems.ts:1435` `relatedIds: ["26"]` (원본 57건에 없음). 데-10과 동일
- **닿을 수 없는 화면: 1건** — `business/apply/page.tsx`(83줄), 링크 0건. 기-24와 동일
- **죽은 코드: 신규 2건** (아래)

---

## 🆕 덤으로 발견 (세 팀 모두 놓친 것)

**1. `components/home/QuickGrid.tsx` (33줄) — 완전히 죽은 파일**
`export default` 있으나 앱 전체 import **0건**. 내부 8개 메뉴는 `HomeFavorites`가 대체 중.

**2. `useUserIdSet` (`lib/userContent.ts`) — export인데 사용처 0**

**3. 🔴 채용 NEW 배지가 목록/상세 불일치 — 디-7 수정이 만든 새 문제**
- 목록 `jobs/page.tsx:120`: `!closed && !userIds.has(job.id) && job.isNew`
- 상세 `jobs/[id]:67`: `!closed && job.isNew` ← **`userIds` 조건 없음**

사용자 등록 공고는 `userContent.ts:191`에서 `isNew: true`를 받는다.
→ **내가 올린 공고는 목록엔 NEW가 없고 상세엔 NEW가 붙는다.**
**→ 2026-08-18 팀장이 수정 완료** (상세에 `!isMine` 추가, 급구 배지도 함께)

**4. `CLAUDE.md`의 verify 설명이 실제와 다름**
"dev와 분리된 안전 검증"이라 적혀 있으나 `verify.cjs:22`가 3000 포트를 죽인다.
**→ 2026-08-18 팀장이 정정 완료**

**5. 기획팀 요약 개수 문제는 이미 자체 정정돼 있었음**
커밋된 보고서에 `🔴6 / 🟡22 / 🟢5 = 33건` + 정정 주석이 반영돼 있음. 추가 조치 불필요.

---

## 👤 사람이 직접 해봐야 하는 목록 (우선순위 순)

> 코드로는 확정할 수 없는 것만. **위 5개가 30분 안에 끝나면서 가장 값지다.**

| # | 무엇 | 시간 | 확인 방법 |
|---|---|---|---|
| **1** | 🔴 **로그인 후 이름** (기-4) | 2분 | 로그아웃 → `/login` → 아무 계정 로그인 → `/write` 글 작성 → 작성자 이름. **"김싱가해"면 확정** |
| **2** | 🔴 **저장 → 로그인 → 어디로** (기-1) | 3분 | 로그아웃 → `/realty` 상세 → "🏷️ 저장" → 로그인 → **홈에 떨어지면 확정** |
| **3** | 🔴 **저장 공간 꽉 찬 상태** (기-5) | 10분 | 사진 5장 글을 여러 개 등록 → 아무 글 "저장" → 개발자도구 Application > Local Storage에서 실제 저장 여부 확인. **코드만으론 절대 확정 불가** |
| **4** | 🔴 **검색어 소실** (기-3) | 2분 | `/search` "EP 비자" → 결과 클릭 → 뒤로가기. 대조군: `/flea`에서 같은 실험 |
| **5** | 🔴 **사진만 올리고 나가기** (기-13) | 3분 | `/write`에 **사진만** 1장 → ✕. 경고 없으면 (나) 확정. 이어서 "지우기" 눌러 사진 남으면 (가) 확정 |
| 6 | 🟡 키보드로 댓글 쓰기 (디-25 잔여) | 3분 | 커뮤니티 상세에서 Tab만으로 댓글창까지. 대조군: `/flea/write`(고쳐짐) |
| 7 | 🟡 확인창 Esc (디-27) | 2분 | 마이페이지 → 설정 → 알림 설정 → Esc. 대조군: 사진 확대창 |
| 8 | 🟡 공유 링크 뒤로가기 (기-9) | 3분 | 상세 주소 복사 → **새 탭**에 붙여넣기 → "← 뒤로" |
| 9 | 🟢 **그림자·여백이 눈에 보이나** (디-22·13·23) | 5분 | `/flea`와 `/news` 두 탭에서 카드 hover 비교. **구분 안 되면 셋 다 닫아도 됨** |
| 10 | 🟡 실기기 터치 (디-20·기-22) | 10분 | 실제 폰에서 사진 삭제 ✕(20px), 상단 아이콘 3개(32px) |

---

## ⚪ 확인하지 못한 것

1. 브라우저 실제 렌더링 — QA팀도 클릭 불가. 위 목록이 대체
2. 빌드 통과 공식 판정 — 1회 실행분(통과) 있으나 재검증 안 함
3. 뉴스 본문 사실 정확성 — 구조적 필드만 검증
4. 업소 16곳 전화번호·주소 유효성
5. 실기기 모바일 터치·키보드
