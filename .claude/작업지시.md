# SORI 작업 지시 기록

> **이 파일은 "누가 무슨 지시를 받았는지"의 기록입니다.**
> 진호님이 저(팀장)에게 시킨 일이 어느 팀으로 갔는지 여기서 확인하실 수 있습니다.
>
> 📌 **VSCode에서 이 파일을 열어두시면**, 제가 지시를 내리는 순간 화면에 바로 반영됩니다.
> (파일이 저장되면 VSCode가 자동으로 다시 읽습니다)
>
> 규칙: 팀에 일을 시킬 때마다 **시키기 직전에** 여기 먼저 적는다. 결과가 오면 상태를 갱신한다.

---

## 🔵 진행 중

### 지시 #14 — 개발팀 (8차 묶음: 🟡 기획 5건)
- **받은 지시**: "남은 묶음도 진행해줘"
- **내린 지시**: 기-7(목록 빈 화면에 다음 행동 없음) / 기-8("내 댓글" 눌러도 댓글 탭이 없음) / 기-14(댓글은 이탈 경고도 임시저장도 없음) / 기-15(수정 화면이 아무것도 안 고쳐도 경고) / 기-16(사진 압축 중 표시 없음 + 초과분 조용히 버림)
- **뉴스 데이터를 건드리지 않는 항목만 골랐다** — 자동 뉴스 PR 40건 처리 방침이 정해지기 전이라 `newsItems.ts` 충돌을 피하기 위함
- **기준 커밋**: `4316997`
- **결과**: 진행 중

---

## ✅ 완료

### 지시 #13 — 개발팀 (7차 묶음: 검색 주소 공유 + SearchAction)
- **기간**: 2026-08-26 (약 17분, 도구 104회)
- **기준 커밋**: `268f770`
- **결과**: **3건 전건 완료** → 커밋 `edb43b3`
- **핵심 판단**: 주소 동기화와 기존 복원(기-3)을 **"주소에 `?q=`가 있으면 주소가 이긴다"** 로 화해시킴. 구현은 `if (urlQuery) return;` **한 줄**. 스크롤 복원은 어느 쪽이든 유지
  - 복원을 아예 빼는 안도 검토했으나, *"검색 아이콘으로 새로 들어왔을 때 아까 검색어가 되살아나는"* 기존 동작이 사라져서 기각 — 최소 변경 원칙
  - 주소 갱신 트리거를 `query`가 아니라 **결과 렌더링과 같은 `debouncedQuery`** 로 잡아, "결과를 클릭할 수 있으면 주소에 이미 검색어가 있다"를 원리적으로 보장
- **자진 신고**: 검색-1이 **최근 검색 기록을 통째로 지우는 회귀**를 유발. 원래 있던 잠복 버그(효과가 `recent`를 옛 값으로 붙잡음)가 "마운트 시 검색어가 항상 비어 있어서" 가려져 있다가 깨어난 것. 스스로 발견·수정
- **팀장 검수**: `/search?q=감자탕` 새 탭 → 검색창·결과 10개 확인 / 두 글자 타이핑에 `history.length` **2 → 2** / `?q=한국&tab=업소` → 상세 → 뒤로 → 검색어·탭·주소·결과 9개 그대로 / 최근 검색어에 기존 항목 보존 확인
- **부수 효과**: 6차에서 보류했던 **SearchAction 추가 완료** — 사실이 된 약속만 넣는다는 원칙 유지

### 지시 #12 — 개발팀 (6차 묶음: 구조화 데이터 1/2)
- **기간**: 2026-08-25 (약 14분, 도구 70회)
- **기준 커밋**: `5ee5fab`
- **결과**: **4건 전건 완료** → 커밋 `268f770`. 8개 주소에서 ld+json 전부 `JSON.parse` 성공
- **성과**: ① **자기가 낸 보안 버그를 스스로 잡음** — 이스케이프 백슬래시 누락으로 XSS 방어가 무력화. tsc·lint 통과하고 화면도 정상이라 **눈으로는 안 보이는 버그**였는데, 악의적 문자열로 시험해 발견 후 수정·재검증 ② **뉴스 저작권 표현 판단** — `citation`이 아닌 `isBasedOn`(파생 관계). publisher는 원 기사가 아니라 "이 한국어 요약문"의 주체라 SORI 유지 ③ **팀장 지시의 사실 오류를 두 번째로 적발** — `/search?q=`가 실제로는 동작하지 않음을 코드로 확인하고 SearchAction을 거부. *"넣으면 구글이 깨진 검색창을 사실로 믿게 된다"*
- **팀장 검수**: 8개 주소 ld+json 파싱·타입 확인, 시드에 없는 id는 사이트 블록만 나가고 HTTP 200 유지 확인. **이스케이프는 소스 원시 바이트로 백슬래시 2개 직접 확인**(검수 중 팀장의 시험 스크립트가 셸 heredoc에서 백슬래시를 먹어 오판할 뻔함)
- **새로 넘어온 것**: `/search`가 `?q=`를 못 읽음(→ 7차) / `communityPosts.ts`의 `comments` 필드가 죽은 데이터(실제 6인데 "28")

### 지시 #11 — 개발팀 (5차 묶음: SEO 기반 공사)
- **기간**: 2026-08-25 (약 17분, 도구 106회)
- **기준 커밋**: `445660a`
- **결과**: **3건 전건 완료** → 커밋 `5ee5fab`
  - `sitemap.xml` **116개 주소**(정적 10 + 시드 상세 106), 개인·작성 화면 0건
  - `robots.txt` 10개 경로 차단 + sitemap 연결
  - 상세 4개에 페이지별 제목 — 전부 같은 제목이던 것이 업소명·직무·매물명·물건명으로
- **성과**: ① **가장 위험한 리팩터를 가장 안전하게** — 옮긴 4개 파일이 원본과 **함수명 1줄만** 다름(팀장이 diff로 직접 확인) ② 시드에 없는 id(사용자 등록분) 4개 전부 HTTP 200 — 서버 404 사고 없음 ③ 본보기를 베끼다 **커뮤니티의 기존 버그 발견**(`커뮤니티 · SORI · SORI`) — 팀장이 함께 정정
- **팀장 검수**: `curl`로 robots·sitemap 실응답 확인, 상세 6개 `<title>` 확인, 원본 대비 diff 직접 실행
- **개발팀 자진 신고**: 검증용 저장소 키 4개를 **이전 내용 확인 없이 덮어씀**. Playwright 전용 브라우저라 실피해 없음. → **지시서에 "저장소를 덮어쓰기 전에 먼저 읽는다" 규칙 추가 예정**
- **새로 넘어온 것**: `OwnerActions.tsx:30-32` 삭제 직후 150ms 경합으로 "앱을 불러오지 못했어요"가 뜰 수 있음(대조 실험으로 기존 문제임을 증명) / `/community/tag`·`/author`는 sitemap 미포함

### 지시 #10 — 개발팀 (4차 묶음: 긴급 3건)
- **기간**: 2026-08-25 (약 16분, 도구 85회)
- **기준 커밋**: `86785cf`
- **결과**: **3건 전건 수정** → 커밋 `445660a`
  - 뉴스 10개 기사 `**` **84개 → 0개**, `<strong>` 0 → 42개, 표·목록·링크 회귀 0건
  - 부동산 시한폭탄 해체 (`lib/realtyStatus.ts` 신설, 시계를 9/5로 앞당겨 검증)
  - 우측 패널 만료 공고 필터 — 목록·검색에 이은 세 번째 지점 통일
- **성과**: ① **판단을 끝까지 밀었다** — 부동산 날짜를 (가)데이터 수정이 아닌 (나)화면 계산으로. 근거가 팀장보다 정확했다: *"사용자 등록 매물은 자유 입력이라 우리가 고칠 수도 없다. 데이터만 미루면 시드는 네 번째가 예약되고 사용자 매물은 영영 안 고쳐진다"* ② 굵게를 넣으면서도 **Linkify를 조각 단위로 살려** 링크·전화 감지를 지켰다 ③ 벼룩은 글꼴이 달라 파서를 합치지 않고 **조각만 공유** — 화면 모양 유지 확인(14.08px/13.12px 그대로)
- **팀장 검수**: `renderMarkdown` 전문 + 신규 `realtyStatus.ts` 검토. **소제목 판정을 좁힌 변경이 현재 데이터에 미치는 영향이 0줄**임을 스크립트로 직접 확인. 브라우저에서 `/news/29` 별표 0·strong 4·목록 5·외부링크 2 재확인
- **새로 넘어온 것**: 우측 패널이 최신순 정렬이 아님 / 우측 패널에 '마감' 표시 없음 / 부동산 id2 등록일보다 입주일이 앞섬

### 지시 #9 — 개발팀 (3차 묶음: 🔴🟡 데이터 5건)
- **기간**: 2026-08-24 (약 19분, 도구 128회)
- **기준 커밋**: `14f2ed0`
- **결과**: **5건 전건 수정** → 커밋 `86785cf`
  - 채용 마감 3/3 → 0/3, NEW·급구 배지 0개 → 2개 부활 / 댓글 라벨 4개 → 6개 / 검색 만료공고 1 → 0 / 부동산 2건 "즉시 입주 가능" / 뉴스 NEW 판정을 `publishedAt` 기준으로 교정
- **성과**: ① **QA가 못 짚은 두 번째 집계 지점 발견** — `comments.ts`만 고쳤을 때 화면 라벨은 그대로였고 실제 표시는 `CommentSection.tsx:415`에서 나왔다 ② **날짜 썩음의 뿌리 규명** — `format.ts`의 `SEED_EPOCH`(2026-07-16) 고정값 기준으로 시드 상대시간이 환산된다. 병명은 "오래됐다"가 아니라 "절대 날짜만 기준시각보다 과거에 박혀 있다" ③ **뉴스 날짜 조작을 거부** — 실제 출처가 달린 기사라 게시일을 지어낼 수 없다며 제품 결정으로 되돌려 보냄 (팀장·진호님 판단: 규칙 그대로 유지)
- **팀장 검수**: diff 전문 + `SEED_EPOCH` 실재 확인, 실험용 임시값 원복 확인. 브라우저 재확인 — `/community/1` "댓글 6개", `/jobs` 마감 0·NEW 1·급구 1
- **진호님 결정**: NEW 배지 규칙은 **"게시 3일 이내" 그대로 유지.** 배지가 안 보이는 것은 콘텐츠가 오래됐다는 정직한 신호로 둔다

### 지시 #8 — 개발팀 (2차 묶음: React 경고 한 뿌리)
- **받은 지시**: "① React 경고 → ② 남은 🔴·🟡 → ③ 🟢 순서로 진행해줘"
- **내린 지시**: 상태 업데이터 **안에서** localStorage 쓰기 + StorageEvent 발송을 하는 3개 파일 정정
- **본보기가 이미 저장소 안에 있었다**: `storage.ts:42-55`(주석까지) · `favorites.ts:74-82` · `CommentSection.tsx`의 `submitComment`
- **기간**: 2026-08-21 (약 20분, 도구 149회)
- **기준 커밋**: `bfd46fe`
- **결과**: **3파일 수정** → 커밋 `14f2ed0`. 경고 4곳(로그아웃·프로필저장·댓글수정·댓글삭제) **전부 1건 → 0건**
- **성과**: ① 측정 방법이 정확했다 — React가 경고를 **컴포넌트당 한 번만** 찍는 성질을 알고 시나리오마다 새로고침을 넣음 ② **잠재 버그 차단** — `prev` 대신 `read()`를 쓰게 되어, 하이드레이션 전 `login()` 호출이 저장된 이름·비자를 지우던 위험이 사라짐 ③ 답글 갈래는 경고가 안 나는 이유(`writeReplies`는 이벤트를 안 쏨)를 밝히고 "0 → 0"으로 정직하게 보고
- **팀장 검수**: diff 전문 + `read()` SSR 안전성 확인. **직접 재현** — `name:"기존이름"`·`visa:"EP"` 심어두고 다른 이메일로 로그인 → 보존 확인. 로그아웃·새로고침 유지·경고 0건 재확인
- **미해결로 넘긴 것**: `writeReplies`만 이벤트를 안 쏴 답글이 탭 간 동기화 안 됨 / 삭제된 댓글의 답글이 저장소에 고아로 남음

### 지시 #7 — 개발팀 (신설, 1차 묶음)
- **받은 지시**: "다음으로 뭘 하면 좋을지 사장으로서 계획해봐" → 팀장 제안 중 **개발팀 채용** 선택
- **왜 채용했나**: 읽기 전용 팀 4개가 110건을 찾아내는 동안 **고치는 손이 팀장 하나**였다. 병목 제거가 목적
- **지시서**: `.claude/agents/fixer.md` (신규)
- **권한**: 읽기 + Bash + **`src/` 쓰기** + Playwright MCP
- **안전장치 5개**: ① 3~5건 묶음 ② 커밋 금지(팀장 diff 검수) ③ `tsc`+`lint` 관문 ④ 브라우저 전후 확인 ⑤ 시드 회귀 확인
- **기간**: 2026-08-20 (약 21분, 도구 145회)
- **기준 커밋**: `e96e1a4`
- **결과**: **4건 전건 수정** (데-7 / 데-8 / 데-9 / 기-12) → 커밋 `ff43865`
  - 파일 3개만 변경, tsc·lint 통과, 커밋 안 함(규칙 준수)
  - 브라우저에 남긴 데이터 0건 — 검증용 업소 등록 후 앱 삭제, 로그아웃, 조회 기록까지 원복
- **성과**: ① **팀장 지시 오류 적발** — "데-20"이 두 보고서에서 다른 항목을 가리켜(QA 데-20 = 죽은 알림 데이터 / 데이터팀 20번 = 조회수), 엉뚱한 파일을 건드리지 않고 되물음 ② 데-9에서 **숫자만 고치면 생길 새 문제**(작성 1인데 "글이 없어요")를 스스로 발견해 같은 파일 안에서 함께 처리, 범위 확장을 명시 보고 ③ `auth.ts:63-77` React 경고 신규 발견(범위 밖이라 미조치)
- **사고**: 없음. 안전장치 5개 전부 작동 확인
- **팀장 검수**: diff 전문 확인 — `toNum`·`formatCount` 실재, 계산식이 `MetricRow.tsx:53`과 동일, 저장소 키 정확. 1440px 홈에서 조회수 11개 전부 축약 형식·쉼표 0개 재확인

### 지시 #6 — 데이터팀 (작성 폼 ↔ 카드 ↔ 상세 3자 일치 점검)
- **받은 지시**: "글 작성 내용과 카드 내용, 상세 내용 일치하지 않는 게 있는지 담당 팀한테 시켜서 확인하라고 해"
- **내린 지시**: 5개 버티컬에서 작성 폼·카드·상세·저장구조·시드데이터 전수 대조. 4가지 불일치 유형으로 분류
- **지시서**: `.claude/agents/data-check.md` + 확장 범위
- **권한**: 읽기 + Bash(읽기 전용) + Playwright MCP + 보고서 파일 1개만 쓰기 허용
- **기간**: 2026-08-19 (약 31분, 도구 161회)
- **결과**: **21건** (가 유령항목 5 / 나 버려지는입력 4 / 다 카드↔상세 1 / 라 형식·빈값 10 / 참고 1) — 🔴4 🟡9 🟢8 → `.claude/reports/2026-08-19-작성폼일치.md`
- **성과**: 5개 버티컬에 **실제로 글을 하나씩 등록해** 검증(전부 앱 삭제 버튼으로 정리). 부동산 `highlights`와 **같은 구조의 유령 항목을 채용에서 추가 발견**(회사 소개)
- **사고**: 없음. 코드 무수정, verify/build/dev 미실행
- **팀장 표본 검증**: 🔴 4건 전부 코드 대조 — `userContent.ts:78·140·259`(작성자 "나"), `:323-347`(업소 photos 누락, 벼룩 `:147`·부동산 `:264`는 넘김), `:179`(companyDesc 빈값), `jobs/write:152-155`(연봉 한쪽만 입력 시 깨짐) — **전부 일치**
- **팀 보고 중 틀린 것 1건**: "`.playwright-mcp/`가 gitignore에 없다"는 **오류**. 상위 `.gitignore:2`가 정상 무시 중이며 `git check-ignore`로 확인. `sori/` 안에는 흔적 0건

### 지시 #5 — QA팀 (2회차, 브라우저)
- **받은 지시**: "QA팀 2회차를 브라우저로 돌려줘. 89건 실제로 눌러서 재현 확인 + 세 폭 스크린샷"
- **내린 지시**: 89건(데이터24/디자인27/기획33 = 84 + 직접검증5) 전건 재판정. 코드 대조에 더해 **실제 브라우저로 눌러 재현 확인**. 390/1024/1440 세 폭 스크린샷. 확인 절차 자체도 판정(임무 1-B)
- **지시서**: `.claude/agents/qa-check.md` (임무 3 브라우저 신설분 포함)
- **권한**: 읽기 + Bash(읽기 전용) + Playwright MCP + 보고서 파일 1개만 쓰기 허용
- **기간**: 2026-08-19 (약 44분, 도구 247회)
- **기준 커밋**: `c2534ec`
- **결과**: **89건 판정** (✅67 / ❌2 / ⚠️19 / ⚪1) → `.claude/reports/2026-08-19-QA2회차.md`
  - 브라우저 재현: 🖥️재현됨 20 / 재현 안 됨 11 / 코드 판정만 58
  - 스크린샷 38장 (390:12 / 1024:13 / 1440:13) → `../.playwright-mcp/qa2/` (gitignore 처리됨)
- **성과**: ① **1회차 분류 오류 13건 적발** — "이미 고쳐짐"을 ✅확인 칸에 넣어 살아 있는 문제 수를 부풀렸음. 실제 1회차 ✅는 81이 아니라 68 ② 기-13 근본원인 재특정(용량 문제 아님, `write/page.tsx:114`가 사진만 있는 draft를 삭제) ③ 기-3 확인 절차가 **수행 불가**였음을 발견(「EP 비자」 검색 결과 0건) ④ 신규 4건 발견
- **사고**: 없음. 코드 무수정, dev 서버 생존, `verify` 미실행 확인
- **팀장 표본 검증**: 기-13 근본원인(`write/page.tsx:62`·`:114`), 신규발견1(부동산 6건 `**` 노출 + `realty/[id]:194` 원문 렌더), 벼룩 처리 로직(`flea/[id]:55`) 직접 대조 — **전부 일치**

### 지시 #4 — QA팀
- **받은 지시**: "1번부터 하고 2번 진행해줘"
- **내린 지시**: 기존 84건을 하나씩 코드와 대조해 ✅확인/❌틀림/⚠️낡음/⚪판정불가 판정. 정적 건강도 점검. 사람이 직접 해봐야 하는 목록 정리.
- **지시서**: `.claude/agents/qa-check.md`
- **권한**: 읽기 + Bash(읽기 전용)
- **기간**: 2026-08-18 (약 17분, 도구 108회)
- **기준 커밋**: `02995a5`
- **결과**: **84건 판정** (✅81 / ❌2 / ⚠️1 / ⚪0) → `.claude/reports/2026-08-18-QA재판정.md`
- **성과**: 팀장의 실수 2건 적발 — ① 디-7 수정이 만든 채용 NEW 배지 목록/상세 불일치 ② CLAUDE.md의 verify 설명 오류. 둘 다 즉시 수정함
- **사고**: 정정 지시 전 `npm run verify` 1회 실행 → dev 서버 종료. **팀장 지시 오류가 원인.** 지시서·CLAUDE.md 정정 완료

### 지시 #3 — 기획팀
- **받은 지시**: "이번엔 기획팀도 만들어보자"
- **내린 지시**: 사용자 관점 점검 8항목 (첫 진입 / 막다른 길 / 되돌릴 수 없는 실수 / 로그인 벽 / 말이 통하는가 / 모바일 / 기다림과 실패 / 흐름)
- **지시서**: `.claude/agents/ux-review.md`
- **권한**: 읽기만 (Read, Grep, Glob)
- **기간**: 2026-08-18 (약 17분)
- **결과**: **33건** (즉시 6 / 검토 22 / 참고 5) → `.claude/reports/2026-08-18-사용자관점.md`
- **후속**: 전건 미조치. 진호님 승인 대기
- **품질**: ⚠️ 요약 개수 오류 1건 (18 vs 실제 22) — 지시서에 자가 검산 규칙 추가함

### 지시 #2 — 디자인팀
- **받은 지시**: "디자인 감사원 우선 만들어줘"
- **내린 지시**: 디자인 일관성 점검 7항목 (버튼 / 카드 / 배지 / 여백·타이포 / 상태 화면 / 공용 컴포넌트 미사용 / 접근성)
- **지시서**: `.claude/agents/design-audit.md`
- **권한**: 읽기만
- **기간**: 2026-08-17 (약 12분)
- **결과**: **27건** (즉시 8 / 검토 16 / 참고 3) → `.claude/reports/2026-08-17-디자인일관성.md`
- **후속**: 🔴 8건 수정 완료 (커밋 74a1a84 5422b60 f7c84e4 c6399ef 0d8d600 1805fdf)
- **품질**: ✅ 제 지시서의 오류(`EmptyState` 오기재)를 잡아냄 / ⚠️ 그림자 개수 미세 오차 1건

### 지시 #1 — 데이터팀
- **받은 지시**: "1번부터 진행해줘" (직원 1호 만들고 검사)
- **내린 지시**: 데이터 정합성 점검 4항목 (개수 표시 / 카드↔상세 지표 / 6개 버티컬 처리 차이 / 데이터 무결성)
- **지시서**: `.claude/agents/data-check.md`
- **권한**: 읽기만
- **기간**: 2026-08-17 (약 12분, 파일 101개 열람)
- **결과**: **24건** (즉시 6 / 검토 10 / 참고 8) → `.claude/reports/2026-08-17-데이터정합성.md`
- **후속**: 🔴 6건 수정 완료 (커밋 6233fa7 28adfdb a40f391 bff908e)
- **품질**: ✅ 표본 검증에서 오류 0건

---

## 🔍 진호님이 직접 하신 일 (팀이 못 한 것)

### 직접 검증 — 2026-08-18
- **한 일**: QA가 정리한 "사람이 직접 해봐야 하는 목록" 5건을 실제 브라우저에서 수행
- **소요**: 약 20분
- **결과**: **5건 모두에서 팀 지적·절차의 결함 발견** → `.claude/reports/2026-08-18-직접검증.md`
  - 🔴 직접검증-1 중간 폭 화면에서 검색 진입점 0개 — **AI 팀 4개가 못 찾은 것**
  - 🔴 직접검증-2 부동산·벼룩 작성자가 저장은 되는데 화면에 안 나옴
  - 🟡 직접검증-3 기-4 정정 (글은 "나", 김싱가해는 댓글만)
  - 🟡 직접검증-4 글쓰기 임시저장 사진 소실 — 기-5의 실제 재현
  - ⚪ 직접검증-5 저장공간 한계는 백엔드 문제 (나중에)
- **교훈**: **AI 팀은 코드를 읽고, 사람은 화면을 본다. 둘은 서로 다른 것을 찾는다.**
  → 앞으로 팀 점검 후에는 반드시 진호님 현장 확인을 거친다

---

## 🔧 팀 없이 팀장이 직접 한 일

| 언제 | 무엇 | 결과 |
|---|---|---|
| 08-17 | 작업 원칙 6 "최소 변경" 추가 | `f2db390` |
| 08-17 | 데이터 🔴 6건 수정 | 커밋 4개 |
| 08-18 | 디자인 🔴 8건 수정 | 커밋 6개 |
| 08-18 | 지시서 3개에 자가 검산 + 기준 커밋 규칙 추가 | — |
| 08-18 | 표본 12건 사장 검증 (오류 1건 발견) | — |
| 08-18 | QA가 잡은 팀장 실수 2건 수정 (채용 NEW 회귀 · CLAUDE.md verify 오설명) | `38e2727` |
| 08-18 | 직접 검증 5건 기록 + 지시서 4개에 "확인 절차 검증" 규칙 추가 | — |

---

## 📋 대기 중 (지시 안 내림)

- 기획 🔴 4건 수정 (기-1, 3, 5, 6) — 진호님 승인 대기
- 기획 🔴 2건 (기-2 부동산 연락처, 기-4 김싱가해) — **진호님 결정 필요**
- 검토 필요 48건 / 참고 16건
- 개발팀 채용 (`fixer`) — 아직 안 만듦. 지금은 팀장이 직접 수정 중

---

## 조직도

| 팀 | 파일 | 권한 | 상태 |
|---|---|---|---|
| 데이터팀 | `agents/data-check.md` | 읽기 + Bash + **브라우저** | ✅ 2회차 완료 (작성폼 일치 21건) |
| 디자인팀 | `agents/design-audit.md` | 읽기만 | ✅ 1회차 완료 |
| 기획팀 | `agents/ux-review.md` | 읽기만 | ✅ 1회차 완료 |
| QA팀 | `agents/qa-check.md` | 읽기 + Bash + **브라우저(Playwright)** | ✅ 2회차 완료 (89건 판정) |
| 개발팀 | `agents/fixer.md` | 읽기 + Bash + **`src/` 쓰기** + 브라우저 | ✅ 2026-08-20 채용 |
